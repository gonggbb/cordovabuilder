依赖安装到工作区根目录： 明确指定 -w 或 --workspace-root 参数：

pnpm add/remove -D vitepress@next -w

可以设置 ignore-workspace-root-check 配置项为 true，但这可能会导致误操作。可以通过以下方式配置：

pnpm config set ignore-workspace-root-check true


npx markdown-link-check docs/**/*.md